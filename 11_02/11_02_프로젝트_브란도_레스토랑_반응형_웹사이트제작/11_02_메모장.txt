;(function(window,document,$,undefined){ //즉시 실행 함수 코드
    //객체(함수와 속성(property)이 함께 포함) 함수 : 
    var brando = {//객체 리터럴 표기법 = 객체 선언과 동시에 값을 지정(헤더,섹션13개,푸터의 변수가 중복되어도 사용할 수 있게 하려고 씀)
        a:0,//정수형변수 // 속성:값 = perperty:value                                                              
        b:'is', //문자형변수 //속성:function() = 익명함수(리터럴함수) //이땐 속성이 아니고 "메소드"가 됨
        c:true, //논리변수
        init:           function(){ //리터럴함수 : 최초로 실행할 함수 // 리터럴:변수를 선언하면서 동시에 값을 지정해 주는 표기방식
            brando.headerFn();
        },//브란도 레스토랑 전체에서 최초에 실행할 js
        headerFn:       function(){//여기서 변수 a를 사용했다 하더라도
            //console.log(brando.a, brando.b, brando.c); 앞에 객체이름 쓰기 귀찮아서 👇
            //console.log(this.a, this.b, this.c);
            var that = this;//이거좋은방법!!
            console.log(that.a, that.b, that.c);

        },//헤더의 js (brando.headerFn.메소드~ 식으로 사용하면됨)


        section01Fn:    function(){
            var cnt=0;
            console.log("section01Fn", cnt );//여기서 변수 cnt를 사용했다 하더라도
        },
        section02Fn:    function(){
            var cnt=-1;
            console.log("section02Fn", cnt );//여기서 변수 cnt는 전혀 다름 = 변수의 중복성 방지
        },